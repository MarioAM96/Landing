/** @type {import('next').NextConfig} */
const nextConfig = {
    //output: 'export',
    //basePath: '/landing' //Path Base para produccion
    images: {
        domains: ['fibramax.ec'],
      },
};

module.exports = nextConfig;
